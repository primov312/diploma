package com.rocketcredit.backend.users;

import org.springframework.data.jpa.repository.JpaRepository;

public interface DemoFinancialProfileRepository extends JpaRepository<DemoFinancialProfileEntity, Long> {
}
